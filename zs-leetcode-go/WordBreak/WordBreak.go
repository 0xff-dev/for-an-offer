package WordBreak

import "bytes"

func wordBreak(s string, wordDict []string) bool {
	buf := bytes.NewBufferString("")
	for index := 0; index < len(wordBreak); index = (index + 1) % len(wordBreak) {

	}
	return false
}
